# WaveMat M0 seam gate

Status: `host_fixture_correctness` only. This PR does not enable WaveMat and is
not an end-to-end performance result.

## Scope

This file is the first reviewable artifact for the #12 M3 seam gate. It fixes
the carrier pointer at the current parent `vendor/vllm` gitlink, records the
provisional call-chain audit, and adds a host-side fail-closed generation/digest
fixture. It does not implement the WaveMat generation-tagged readiness runtime
path.

## Carrier checkpoint

- parent commit: `375d3a8a10610e13d3ca1f46d0046f0a7282373e`
- `vendor/vllm` gitlink at parent HEAD:
  `68b8be04493d39d5706f3d0d18f465f5eab947c4`
- previously inconsistent docs:
  - `README.md`: `f8efeebe900e638f178e3b460f50cc750054fa17`
  - `HANDOFF.md` / M2 historical record: `475ea49`

This PR treats the parent gitlink as the candidate carrier pin for M3. The
M2 historical carrier `475ea49` is preserved as historical evidence and is not
rewritten. The submodule must still be initialized and its actual content
checked before claiming that `68b8be0` contains the required layerwise API.

## Provisional exact call chain

Exact line numbers are not yet reproducible in this checkout because
`vendor/vllm` is not initialized. The following paths are the target audit
surface, not a claim of verified behavior:

| Stage | Target path | Class / function | M0 gate question |
|---|---|---|---|
| repo plugin | `src/vllm_kv_materialization/plugin.py` | `register_plugin` | Does current seam reach per-layer KV? |
| OpenAI chat entry | `vllm/entrypoints/openai/chat_completion/serving.py` | `OpenAIServingChat.create_chat_completion` | Request and header binding only |
| render | `vllm/entrypoints/serve/render/serving.py` or older `serving.py` | `OpenAIServingRender.render_chat` | Prompt and runtime plan creation point |
| sampling params | `vllm/entrypoints/openai/chat_completion/protocol.py` | `ChatCompletionRequest.to_sampling_params` | `kv_materialization_runtime_control` injection |
| engine submit | `vllm/v1/engine/async_llm.py` / older `async_llm_engine.py` | `AsyncLLM.add_request` / `AsyncLLMEngine.add_request` | Request enters scheduler |
| scheduler | `vllm/v1/engine/core.py` / older `llm_engine.py` | `EngineCore` / `LLMEngine.step` | Prefill step and KV block allocation |
| worker runner | `vllm/v1/worker/gpu_model_runner.py` / older `model_runner.py` | `GPUModelRunner.execute_model` | Per-layer loop and hook surface |
| Ascend platform | `vllm_ascend/platform.py` | `NPUPlatform.pre_register_and_update` / `check_and_update_config` | Graph-mode configuration |
| Ascend runner | `vllm_ascend/worker/model_runner_v1.py` or `vllm_ascend/worker/v2/model_runner.py` | `NPUModelRunner` / `ModelRunner.execute_model` | Actual Ascend execution path |
| attention/KV | `vllm_ascend/attention/attention_v1.py` or `_310p/attention/attention_v1.py` | `AscendAttentionBackend` / `AscendAttentionBackend310` | Real per-layer load/consume seam |
| KV allocator | vLLM core, exact file pending pin | `KVCacheManager` / `KVCacheConfig` / `KVCacheSpec` | Address stability under graph replay |

The first fact to settle in M0 is whether a real per-layer load/consume seam is
reachable. If the carrier only exposes whole-model sync or eager-only operation,
the seam gate stops before any WaveMat mechanism patch.

## Gate checklist

- [ ] Initialize `vendor/vllm` and record exact carrier commit + line numbers.
- [ ] Confirm KVConnector V1 layerwise load/consume exists in the pinned carrier.
- [ ] Confirm vLLM-Ascend Layerwise KV Pool and layerwise prefetch exist in the
      pinned carrier.
- [ ] Trace `wait_for_layer_load` / Ascend layer-ready path to attention
      consume.
- [ ] Trace `FULL` and `FULL_AND_PIECEWISE` graph-piece, host sync, and event
      wait paths.
- [ ] Prove or disprove a reproducible graph-induced overlap loss, sync bubble,
      or replay correctness gap on 910B2 graph mode.
- [ ] Keep `VLLM_WAVEMAT_ENABLE=0` and native path default until M0 go.
- [ ] Do not promote host fixture, simulation, dry-run, or projected numbers to
      end-to-end performance evidence.

## Host-side failure injection

The first PR includes a deterministic, stdlib-only fixture:

- partial layer range
- stale generation
- duplicate consume
- ABA generation reuse
- load failure
- digest mismatch
- reuse before ACK
- crash/recovery orphan detection

All cases are expected to fail closed. Raw fixture results are generated by:

```bash
PYTHONPATH=src python3 scripts/run_wavemat_gate_selfcheck.py \
  --output docs/wavemat/results/m3_pr1_seam_gate_raw.json \
  --manifest-output docs/wavemat/M3_PR1_MANIFEST.json
```

The fixture result does not imply that a real Ascend per-layer consumer seam
exists or that WaveMat has any end-to-end performance benefit.
