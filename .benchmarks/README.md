# Benchmark Workspace

This repository's paper-study and live-serving experiment drivers remain under
`paper/kv_materialization_control/experiments`, while `.benchmarks/` serves as
the stable root-level home for standardized shared workload smoke artifacts.

`make shared-workloads-smoke` writes its report under `.benchmarks/results`.